import { StyleSheet } from "react-native";
import { govuk_colour } from "../constants/colours";

const styles = StyleSheet.create({
  scrollViewContainer: {
    flexGrow: 1,
    backgroundColor: govuk_colour.white,
  },
});

export default styles;
