import { StyleSheet } from "react-native";
import { govuk_font_size_xl } from "../../../constants/styles";

const styles = StyleSheet.create({
  h1: {
    marginBottom: 50,
    maxWidth: 240,
    fontSize: govuk_font_size_xl,
  },
});

export default styles;
