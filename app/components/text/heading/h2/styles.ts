import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    h2: {
        paddingTop: 20,
        marginBottom: 30,
        maxWidth: 320,
        fontSize: 36,
    },
})

export default styles;
