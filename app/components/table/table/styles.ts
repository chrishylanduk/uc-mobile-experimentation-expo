import { StyleSheet } from "react-native";
import { GOVUK_BORDER_COLOUR } from "../../constants/colours";

export const govukTableStyle = StyleSheet.create({
  border: {},
  style: { borderBottomWidth: 1, borderBottomColor: GOVUK_BORDER_COLOUR },
});
